base:
  '*':
    - base
    - docker

# Install salt
# sudo apt-get install salt-minion
#
# Run it
# sudo salt-call --local --force-color --config /home/ubuntu/src/saltify/config/ state.highstate | less -R
